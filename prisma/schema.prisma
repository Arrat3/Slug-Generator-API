generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Slug{
  id Int @default(autoincrement()) @id
  originalString String
  slug String @unique
}
