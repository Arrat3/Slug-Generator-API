generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Slug{
  id Int @default(autoincrement()) @id
  originalString String
  slug String @unique
}
